interface:
  display_name: "Operate Decoupled-Spec Servers"
  short_description: "Operate a Ray decoupled-spec server fleet"
  default_prompt: "Use $operate-decoupled-spec-servers to validate, launch, inspect, and stop the unified Ray server fleet."
policy:
  allow_implicit_invocation: true
