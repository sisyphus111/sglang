interface:
  display_name: "Run Decoupled-Spec Benchmark"
  short_description: "Run one traceable decoupled-spec benchmark"
  default_prompt: "Use $run-decoupled-spec-benchmark to run one fully traceable benchmark case from the four YAML configs."
policy:
  allow_implicit_invocation: true
